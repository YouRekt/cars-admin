import SidebarButtonProvider from "@/components/SidebarButtonProvider";

const Administrators = () => {
	return <SidebarButtonProvider>Administrators</SidebarButtonProvider>;
};
export default Administrators;
